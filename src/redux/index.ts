import {
    selectQuestion,
    selectResults,
    fetchQuestions,
    setAnswer,
    reset,
  } from "./commonReducer";
  
  export { selectQuestion, selectResults, fetchQuestions, setAnswer, reset };
  